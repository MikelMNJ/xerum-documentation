import React from 'react';
import SectionButton from './SectionButton';

const Buttons = props => {
  return (
    <section id="buttons">
      <SectionButton />
    </section>
  );
};

export default Buttons;